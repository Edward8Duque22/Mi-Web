function enviarFormulario() {
    var nombre = document.getElementById("nombre").value;

    // Aquí puedes hacer lo que desees con los datos, como enviarlos a través de AJAX
    // En este ejemplo, simplemente los mostramos en una alerta
    alert("Nombre: " + nombre);
}